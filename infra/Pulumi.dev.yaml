config:
  aws:region: ap-southeast-1
  aws:defaultTags:
    tags: 
      Environment: dev 
      GitRepo: web-server 
      ManagedBy: Pulumi 
  workshop-config:githubOrg: workshop-github-org  # just a label
  workshop-config:org: workshop-resources-org # in aws account resources will be named as workshop-resources-org-dev-web-server-vpc
  workshop-config:pulumiOrg: workshop-pulumi-org  # just a label

  # workshop-config: namespace 

