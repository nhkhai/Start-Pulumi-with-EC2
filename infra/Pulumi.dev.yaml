config:
  aws:region: us-west-2 # ap-southeast-1
  aws:defaultTags:
    tags: 
      Environment: workshop # dev
      ManagedBy: Pulumi
      GitRepo: Start-Pulumi-with-EC2
  workshop-config:githubOrg: this-is-github-org   # just a label
  workshop-config:org: workshop-resources-org # in aws account resources will be named as workshop-resources-org-dev-web-server-vpc
  workshop-config:pulumiOrg: this-is-pulumi-org  # just a label

  # workshop-config: namespace 

